import VendorPagePlaceholder from "@/components/vendor/VendorPagePlaceholder";

const VendorStoreSettings = () => (
  <VendorPagePlaceholder title="Store Settings" description="Profile, delivery zone, and branding." sprint="Sprint D" />
);

export default VendorStoreSettings;
